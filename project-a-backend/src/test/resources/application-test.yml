spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/project_a_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    validate-on-migrate: true

  mail:
    host: localhost
    port: 3025
    username: test
    password: test
    from: no-reply@projecta.local
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

  task:
    scheduling:
      enabled: false

security:
  jwt:
    secret-key: isN5Z1EinzeD2JyjXq6SZqoBd+QpOJMxZsBHwfhXHAA=
    access-token-expiration: 900000
    refresh-token-expiration: 2592000000
    refresh-token-cookie-name: refresh_token

application:
  file:
    uploads:
      file-output-path: ./test-uploads

  frontend:
    url: http://localhost:4200

server:
  port: 0
